"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const cucumber_1 = require("cucumber");
const testRail_1 = require("../../src/utils/testRail");
const testRailHandler_1 = require("../../src/utils/testRailHandler");
const config_1 = __importDefault(require("../../src/config/config"));
require("dotenv").config();
// Credentials for the TestRail API
let testRail = new testRail_1.TestRail(`${process.env.TESTRAIL_USERNAME}`, `${process.env.TESTRAIL_APIKEY}`, config_1.default);
let testRailHandler = new testRailHandler_1.TestRailHandler(testRail, config_1.default);
// Hooks: Use for setup and teardown of the environment before and after each scenario.
// Runs once before all the scenarios.
cucumber_1.BeforeAll(async function () {
    await testRailHandler.BeforeAllHandler();
});
// Runs before the first step of each scenario.
cucumber_1.Before(async function (params) {
    await testRailHandler.BeforeHandler(params);
});
// Runs after the last step of each scenario, even when steps have failed, are undefined, pending or skipped.
cucumber_1.After(async function (params) {
    await testRailHandler.AfterHandler(params);
});
//# sourceMappingURL=hooks.js.map