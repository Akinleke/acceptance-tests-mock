export declare class TestRailHandler {
    private testRail;
    private config;
    private projectId;
    private sprintSuiteId;
    private sprintSectionId;
    private sprintRunId;
    private sprintCase;
    private caseId;
    private regressionSuiteId;
    private regressionSectionId;
    private regressionRunId;
    private regressionCase;
    private regressionCaseId;
    constructor(testRail: any, config: any);
    BeforeAllHandler: () => Promise<void>;
    BeforeHandler: (params: any) => Promise<void>;
    AfterHandler: (params: any) => Promise<void>;
}
